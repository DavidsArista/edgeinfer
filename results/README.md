# Results directory

Raw benchmark runs are stored locally and gitignored. Small sanitized samples may be placed under `results/samples/` for documentation.

No performance numbers should be invented or committed without measured raw JSON/JSONL backing them.
