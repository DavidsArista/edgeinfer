# Models directory

Local model artifacts and manifests live here. Contents are gitignored except this README and small example manifests.

Do not commit weights, SDK binaries, or large generated files.
